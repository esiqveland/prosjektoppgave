TODO

ideas:

abstract:
siste setning, var sikker på om vi kan si peak load er promising eller om det er failure.

fikse engelsk tallsystem (komma på lange tall for å skille tusinger)

optimize c-port:
    ekkel setning:
        This proved it both challenging and useful to control what exactly is present in memory at every moment, in addition to the extra work of having to control memory allocation.

results:
    finn cpuload ved breakpoint med 1 load distr, kap 7.1.1

results: mac vs pi:
    brukt kraftigere pc for å teste begge kjernekjøring av search_bin på mac.

experiments.experiences with C and Python
    FIX TABLE LOOK

future work:
try out nodes for indexing work, lots of IO, but too much cpu? Map reduce does not seem to perform well on pis, at least with hadoop.\ref





python2.7 loadgenerator/querygenerator.py target/dictionary.txt target/query_file.txt &

cat /sys/devices/system/cpu/cpu0/cpufreq/scaling_cur_freq

